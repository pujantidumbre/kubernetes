apiVersion: v1
kind: Service
metadata:
  name: myapp-nodeport
  labels: 
    app-node: myapp-node
spec:
  selector:
    app-type: backend
    app: my-app
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30010
